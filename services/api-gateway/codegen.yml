overwrite: true
schema: "schema.graphql"
documents: null
generates:
  src/graphql/types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  # ./graphql.schema.json:
  #   plugins:
  #     - "introspection"
